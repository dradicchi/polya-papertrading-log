# Retired / frozen instance

**Frozen on 2026-07-30.** This instance no longer receives new events, reports,
positions snapshots or OpenTimestamps anchors. Its historical event stream and
proofs remain in place, unedited, for audit continuity — the append-only Git
history and the existing timestamps still verify exactly as before.

This instance is **not part of the active track record**. The current track
record consists of three instances: `canonical` (reference benchmark) and the
`full_overrides` (CALL) + `put_full_overrides` (PUT) legs of the primary combined
short-volatility book. See the repository [`README.md`](../../README.md).

**What this was:** the original single-instance PT v2 baseline (go-live 2026-04-13), retained because it includes the early-May 2026 bear-to-bull mean-reversion event window (PM-001).
